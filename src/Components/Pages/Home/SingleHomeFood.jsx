import React from 'react';

const SingleHomeFood = ({ food }) => {
    console.log(food);
    const { donatorName, expireDate, foodImage, foodName, foodQuantity, pickupLocation } = food
    return (
        <div>
            
        </div>
    );
};

export default SingleHomeFood;